// utilizamos [] para indicar el inicio y el fin de un array, Y usamos comas para separar sus elementos
// se pueden almacenar la cantidad de elementos que queramos sin importar el tipo de dato

let miArray = ['Star Wars', true, 23];

// cada dato de un array ocupa una posicion numerada conocida como indice, la primera posicion de un array es siempre 0
// para acceder a un elemento puntual de un array, nombramos al array y, dentro de los corchetes, escribimos el indice al cual queremos acceder
let pelisFavoritas = ['Star Wars', 'Kill Bill', 'Alien'];

pelisFavoritas[2];// accedemos a la pelicula Alien, el indice 2 del array

// con la propiedad length el numero de elementos que tiene el array

pelisFavoritas.length; // devuelve 3, el numero de elementos del array



